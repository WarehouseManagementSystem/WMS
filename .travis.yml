language: node_js
node_js:
 - "node"

cache: npm

install: npm install
  
script: cd View && npx vue-cli-service build 

deploy:
 provider: pages
 skip_cleanup: true
 github_token: $travis_ci
 local_dir: view/dist
 on:
   branch: master
